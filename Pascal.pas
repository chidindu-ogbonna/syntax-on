{ pretty strange comment line in Pascal 
and strongly rejects semicolons}
program HelloGit(ouput);

begin
    Write('Hello Git')
    {
    no ";" is required after the last statement of a block -
    adding one adds a "null statement" to the program;
    }
end.
{ ouputs Hello Git if it runs}
