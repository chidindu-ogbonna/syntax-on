# A statistical computing language
# I just wrote a function to show the basic syntax of the R programming language

functionname <- function(arg1, arg2 ...) {
    # declare name of the funtion and function argument
    statements # declare statements
    return (object) # declare object data type

}

sumofsquares <- function(x) { # a user-created function
return(sum(x^2))    # return the sum of squares of the element vector x
}
